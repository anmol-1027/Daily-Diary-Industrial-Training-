# Day 02 - Basic Commands of Kali Linux

**Date:** July 2, 2026

## Objective

Learn the basic Linux commands used in Kali Linux for navigation, file management, and system operations.

## Topics Covered

* Introduction to the Linux Terminal
* File and Directory Navigation
* File and Directory Management
* User and System Information Commands
* Basic Terminal Operations

## Theory

### Linux Terminal

The Linux terminal is a command-line interface (CLI) that allows users to interact with the operating system by executing commands. It is one of the most important tools for system administration and cybersecurity tasks.

### Basic Commands Learned

Some of the basic commands introduced during the session include:

* `pwd` – Displays the current working directory.
* `ls` – Lists the files and directories in the current location.
* `cd` – Changes the current directory.
* `mkdir` – Creates a new directory.
* `rmdir` – Removes an empty directory.
* `touch` – Creates a new empty file.
* `cp` – Copies files or directories.
* `mv` – Moves or renames files and directories.
* `rm` – Deletes files or directories.
* `cat` – Displays the contents of a file.
* `clear` – Clears the terminal screen.
* `whoami` – Displays the current logged-in user.
* `history` – Shows previously executed commands.
* `man` – Opens the manual page for a command.
* `echo` – Prints text or variables to the terminal.

## Practical Session

* Practiced navigating between directories using terminal commands.
* Created and removed files and folders.
* Copied, moved, and renamed files.
* Viewed file contents using terminal commands.
* Explored command documentation using the `man` command.

## Key Learnings

* Understood how to use the Linux terminal efficiently.
* Learned the purpose and syntax of commonly used Linux commands.
* Gained confidence in navigating the file system using the command line.
* Performed basic file and directory operations successfully.

## Conclusion

The second day focused on learning the basic commands of Kali Linux. These commands form the foundation for working in a Linux environment and are essential for performing cybersecurity and ethical hacking tasks. Regular practice of these commands will improve speed and efficiency while using Kali Linux.

---
